@model IEnumerable<SportEvents.Domain.Registration>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap">

<h1 class="text-center my-4">My Registration List</h1>

<div class="container">
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegistrationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Participant)
            </th>
            <th class="text-center">Actions</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @item.RegistrationDate.ToString("yyyy-MM-dd")
                </td>
                <td>
                    @item.Status
                </td>
                <td>
                    @item.Event.Description
                </td>
                <td>
                    @item.Participant.Email
                </td>
                <td class="text-center">
                    <a class="btn btn-outline-dark btn-sm" asp-action="Delete" asp-route-id="@item.Id" onclick="return confirm('Are you sure you want to delete this item?');">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
<style>
    div {
        font-family: 'Roboto', sans-serif;
    }
    table th, table td {
        font-size: 1rem; /* Adjust font size as needed */
    }
    h1 {
        font-size: 2.5rem; /* Adjust font size as needed */
        font-weight: 700; /* Bold font weight */
    }
</style>
